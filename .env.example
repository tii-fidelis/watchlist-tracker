# TMDB (The Movie Database) API read access token.
# Get a free one at https://www.themoviedb.org/settings/api (the "API Read Access Token", not the shorter v3 API key).
# Read server-side only (src/lib/tmdb.server.ts) — never exposed to the client bundle.
TMDB_API_TOKEN=
